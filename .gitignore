# Environment secrets
.env
.env.*
!.env.example

# Local build artifacts
/build/
/app
/gobot
/gobot.db
/*.db
/*.sqlite
/*.sqlite3
/*.log
/coverage.out

# Go local caches
/.gocache/

# SQLite data volume (used by docker compose in dev)
/data/

# Local agent/editor metadata
/.agents/
/.codex/

# Sensitive local deploy script with embedded credentials (legacy)
/deploy.py
